version: '3'

services:
  torimies-rs:
    build: .
    restart: unless-stopped
    volumes:
      - .env:/app/.env
      - ./migrations:/app/migrations
      - ./database.sqlite:/app/database.sqlite
